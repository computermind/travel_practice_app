

<div>
    <ul>
    <% @countries.each do |country| %>
        <li><%= country %></li>
    <% end %>
    </ul>
</div>

<div class="container">
    <% @liberia.each do |tourist_attraction| %>
    <div class="row">
        <div class="col-md-3">
            <div class="well well-lg">
                <%= image_tag tourist_attraction, alt: "tourist_attraction", id: "liberia", class: "country" %>
            </div>
        </div>
    </div>
    <% end %>
</div>